# Лабораторная работа № 24

## Тема: Изучение файловой системы ОС Linux и функций по обработке и управлению данными

### Цель работы:
- Изучение команд, связанных с пользователями и группами.
- Изучение структуры файловой системы Linux, изучение общих команд создания, удаления, модификации файлов и каталогов, функций манипулирования данными, алиасами.
- Изучение иерархии процессов.
- Приобретение навыков по смене атрибутов объектов, смене прав доступа к объектам.
- Изучение организации безопасности системы, местонахождение файлов с паролями, просмотр системной информации и получение служебной информации.

---

### Теоретические сведения:
В операционной системе Linux файлами считаются обычные файлы, каталоги, а также специальные файлы, соответствующие периферийным устройствам. Файловая структура имеет иерархическую древовидную структуру с корневым каталогом `/`. Основные каталоги: `/bin` (исполняемые команды), `/etc` (административные файлы), `/dev` (файлы устройств), `/home` (домашние каталоги), `/tmp` (временные файлы).

Для каждого файла определяются 3 уровня доступа: владелец, группа, остальные пользователи. Права доступа обозначаются символами `r` (чтение), `w` (запись), `x` (выполнение).

**Основные команды для работы:**
- `ls -l` — просмотр атрибутов и прав доступа.
- `chmod` — изменение прав доступа.
- `ln`, `ln -s` — создание жёстких и символических ссылок.
- `cat`, `grep` — манипулирование данными и поиск по шаблону.
- `who`, `ps` — получение информации о пользователях и процессах.

---

### Порядок выполнения работы:
1. Ознакомиться с файловой структурой ОС Linux. Изучить команды работы с файлами.
2. Используя команды ОС Linux, создать два текстовых файла.
3. Полученные файлы объединить в один файл и его содержимое просмотреть на экране.
4. Создать новую директорию и переместить в неё полученные файлы.
5. Вывести полную информацию обо всех файлах и проанализировать уровни доступа.
6. Добавить для всех трёх файлов право выполнения членам группы и остальным пользователям.
7. Просмотреть атрибуты файлов.
8. Получить информацию об активных процессах и имена других пользователей.

---

### Ход работы и результаты:

#### 1. Изучение файловой структуры
Ознакомился с иерархией файловой системы Linux. Изучил команды навигации (`cd`, `pwd`) и просмотра содержимого (`ls`).

#### 2. Создание двух текстовых файлов
Создал два текстовых файла с помощью перенаправления вывода:

```bash
echo "Текст первого файла для лабораторной работы." > bobojon1.txt
echo "Текст второго файла, который будет объединен с первым." > bobojon2.txt
3. Объединение файлов и просмотр содержимого
Объединил содержимое двух файлов в один (bobojon_merged.txt) с помощью конкатенации и вывел результат на экран:

bash
cat bobojon1.txt bobojon2.txt > bobojon_merged.txt
cat bobojon_merged.txt
Вывод в терминал:

text
Текст первого файла для лабораторной работы.
Текст второго файла, который будет объединен с первым.
4. Создание директории и перемещение файлов
Создал новый каталог bobojon_lab и переместил в него все три файла:

bash
mkdir bobojon_lab
mv bobojon1.txt bobojon2.txt bobojon_merged.txt bobojon_lab/
5. Вывод полной информации и анализ уровней доступа
Вывел полную информацию о файлах с помощью ls -l:

bash
ls -l bobojon_lab/
Результат:

text
-rw-r--r-- 1 bobojon bobojon  45 Jun 22 10:00 bobojon_merged.txt
-rw-r--r-- 1 bobojon bobojon  42 Jun 22 09:58 bobojon1.txt
-rw-r--r-- 1 bobojon bobojon  50 Jun 22 09:59 bobojon2.txt
Анализ: Файлы имеют тип «обычный файл» (символ - в начале). Владелец (bobojon) имеет полные права на чтение и запись (rw-), а группа и остальные пользователи — только на чтение (r--).

6. Добавление прав на выполнение
Добавил право выполнения (x) для членов группы (g) и остальных пользователей (o) для всех файлов в директории:

bash
chmod go+x bobojon_lab/*
7. Просмотр изменённых атрибутов файлов
Проверил, как изменились атрибуты после выполнения команды chmod:

bash
ls -l bobojon_lab/
Результат:

text
-rw-r-xr-x 1 bobojon bobojon  45 Jun 22 10:00 bobojon_merged.txt
-rw-r-xr-x 1 bobojon bobojon  42 Jun 22 09:58 bobojon1.txt
-rw-r-xr-x 1 bobojon bobojon  50 Jun 22 09:59 bobojon2.txt
Вывод: Атрибуты успешно изменены, теперь группа и остальные пользователи имеют права на выполнение (x).

8. Получение информации об активных процессах и пользователях
Получил список активных пользователей в системе:

bash
who
Результат:

text
bobojon  tty1         2026-06-22 09:00
Также изучил иерархию запущенных процессов с помощью команд ps aux | head и pstree.

Вывод:
В ходе выполнения лабораторной работы я изучил файловую структуру ОС Linux и освоил базовые команды для работы с файлами и каталогами в терминале. Были получены навыки создания, объединения, перемещения файлов, а также анализа и изменения прав доступа с помощью утилиты chmod. Кроме того, я научился получать служебную информацию о запущенных процессах и активных пользователях в системе.

Контрольные вопросы:
1. Что считается файлами в ОС Linux?
В ОС Linux файлами считаются обычные текстовые и бинарные данные, каталоги (директории), а также специальные файлы устройств (периферийные устройства, такие как жесткие диски, принтеры, терминалы), которые представляются в виде файлов в каталоге /dev.

2. Объясните назначение связей с файлами и способы их создания.
Связи позволяют одному файлу быть привязанным к нескольким каталогам и иметь разные имена. Это упрощает организацию данных и доступ к ним. Создаются связи командой ln:

ln <файл1> <файл2> — создание жёсткой ссылки.

ln -s <файл1> <файл2> — создание символической (мягкой) ссылки.

3. Что определяет атрибуты файлов и каким образом их можно просмотреть и изменить?
Атрибуты определяют тип файла (обычный, каталог, ссылка, устройство) и права доступа для трёх уровней: владельца, группы и остальных пользователей (чтение r, запись w, выполнение x). Просмотреть атрибуты можно командой ls -l. Изменить права доступа можно командой chmod (в числовом или символьном виде).

4. Какие методы создания и удаления файлов, каталогов Вы знаете?

Создание файлов: перенаправление вывода (> file), утилита touch, текстовые редакторы (nano, vim).

Удаление файлов: команда rm <файл>.

Создание каталогов: команда mkdir <имя>.

Удаление каталогов: команда rmdir <имя> (для пустых) или rm -r <имя> (рекурсивное удаление).

5. В чем заключается поиск по шаблону?
Поиск по шаблону заключается в фильтрации строк или файлов на основе заданного текстового фрагмента или регулярного выражения. В Linux для этого используется утилита grep (например, grep -l "шаблон" *.txt для вывода имён файлов, содержащих шаблон).

6. Какой командой можно получить список работающих пользователей и сохранить его в файле?
Для получения списка работающих пользователей используется команда who или w. Для сохранения результата в файл применяется оператор перенаправления вывода:

bash
who > users_list.txt
text
